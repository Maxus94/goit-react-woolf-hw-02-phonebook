export const ContactList = (contacts) => {    
//   return (contacts.map(contact => (
//     <li key={contact.id}>
//       {contact.name} {contact.number}
//       <button type="button" onClick={() => this.handleDelete(contact.id)}>
//         Delete
//       </button>
//     </li>
//   )));
};
